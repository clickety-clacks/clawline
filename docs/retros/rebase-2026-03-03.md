# Clawline Rebase Report — 2026-03-03

## Upstream intake summary
Merged upstream tag `v2026.3.2` (`85377a281`) into branch `clawline-rebase-2026-03-03`.

Major upstream patterns adopted:
- WS auth/connect split (`auth-context`, `connect-policy`, `unauthorized-flood-guard`) and client handshake hardening.
- `run.ts` retry/lane/auth-failover/copilot lifecycle restructuring.
- `buildAllowedModelSet` explicit-allowlist trust + synthetic catalog entries.
- defaults/config evolution (`SecretInput` ecosystem and updated default aliasing).
- channel runtime injection wiring and plugin/gateway surface refactors.

## Conflict zones and resolutions
1. `src/agents/pi-embedded-runner/run.ts`
- Took upstream shape and verified lane invariant.
- Preserved deadlock guard: `resolveGlobalLane(params.lane)` (no `?? sessionLane`).

2. `src/agents/model-selection.ts`
- Took upstream `buildAllowedModelSet` behavior with synthetic catalog entries.
- Did not reintroduce fork-only `defaultProvider` branch (behavior covered by upstream strategy).

3. `src/config/defaults.ts`
- Took upstream defaulting logic.
- Preserved fork explicit aliases: `sonnet-4.5` and `sonnet-4.6`.

4. `src/plugin-sdk/index.ts`
- Took upstream SDK surface.
- Re-added fork-required exports:
  - `ClawlineDeliveryTarget`
  - `sendClawlineOutboundMessage`
  - `startClawlineService` / `ClawlineServiceHandle`

5. `src/gateway/call.ts`
- Kept upstream file (no divergence vs upstream tag after merge).

6. `src/gateway/server-methods/agent.ts` (+ test)
- Preserved request-channel send-policy fallback:
  - `channel: entry?.channel ?? requestChannel`
- Kept upstream ACP metadata preservation test and retained fallback regression coverage.

7. `src/config/legacy.migrations.part-3.ts`
- Kept upstream migrations and retained fork migration `clawline -> channels.clawline`.

8. `src/config/types.ts` + `src/config/types.channels.ts`
- Aligned to upstream modular typing pattern.
- Removed core `types.clawline` export and core `ClawlineConfig` channel field.

## Additional merge-adjacent fixes required by test/invariant checks
1. `src/infra/outbound/message-action-params.ts`
- Restored attachment compatibility while retaining upstream media-policy structure:
  - normalize buffer-object payloads to base64 for `sendAttachment`
  - allow loopback media fetches with explicit SSRF allowlist options

2. `src/config/plugin-auto-enable.ts`
- Restored expected Clawline behavior: when configured, register `plugins.entries.clawline.enabled=false` with message `clawline configured, not enabled yet.` (not auto-enabled).

3. `src/clawline/server.test.ts`
- Updated expectations to match DM stream gating invariant (`dmScope`-dependent DM stream creation) and upstream naming (`Global DM`).

4. `src/secrets/resolve.test.ts`
- Increased `timeoutMs` from `500` to `2000` in one no-output fallback test to avoid parallel-run flakiness under heavy load.

## Clawline rewrites/adoptions
- Adopted upstream WS module layout and auth flow composition without changing Clawline-owned `/ws` and `/ws/terminal` service architecture.
- Adopted upstream config modularization and removed hard core coupling to Clawline config types.
- Adopted upstream model-allowlist strategy, preserving fork aliases and observed OAuth usability behavior.

## What could not be cleanly adopted
- No intractable blocker encountered. No escalation required.

## Divergence delta (relative to upstream)
- Clawline-owned trees remain fork additions: `src/clawline/**` and `extensions/clawline/**`.
- Upstream-owned divergence was minimized to required behavior-preservation points and verification/test stability adjustments listed above.
- `src/gateway/call.ts` has no post-merge delta versus upstream tag.

## Verification performed
- Collision/invariant greps:
  - lane deadlock invariant in `run.ts` verified.
  - `buildAllowedModelSet` upstream pattern + synthetic catalog verified.
  - `sonnet-4.5`/`sonnet-4.6` aliases verified.
  - Clawline SDK exports verified in `plugin-sdk/index.ts`.
  - request-channel fallback verified in `server-methods/agent.ts`.
  - core config type decoupling verified (`types.clawline`/`ClawlineConfig` absent).
- Build/test:
  - `pnpm install`
  - `pnpm build` (pass)
  - `pnpm test` (pass): `813 passed, 1 skipped` test files; `6555 passed, 2 skipped` tests.

## Open questions for CLU
1. The `secrets/resolve` no-output timeout case required increasing a test timeout under full parallel load. If desired, this can be revisited with a deterministic fake-timer harness to avoid wall-clock sensitivity.
