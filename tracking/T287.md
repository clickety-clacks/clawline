# T287

## Status
Ready for Flynn verification on branch `clawline-web-parity-layout-batch`.

## Product Truth
Web model/status footer parity follows native footer intent from T243/T252 source context:

- Footer/status indicators expose model, thinking/reasoning, and Fast mode through typed session-control actions.
- The controls are not slash-command chat text.
- Model choices come from provider model catalog data when present.
- The footer stays hidden at the normal resting bottom and fades in only when the user scrolls beyond that resting bottom.

Provider seam tickets are not dependencies for this Web ticket unless a live capability mismatch is independently observed.

## Implementation Notes
Web now passes full `/api/session-status` payloads to the selected chat view. `SessionStatusFooter` renders typed controls for:

- `set_model` using `modelCatalog.models` with the current option marked in the picker.
- `set_thinking` / `set_reasoning` based on status display and capabilities.
- `set_fast_mode` / `set_mode` based on status display and capabilities.

Footer reveal is implemented as an extra trailing scroll region: normal scroll-to-bottom stops at the transcript/typing resting bottom, while manual extra scroll reveals the footer by opacity.

## Evidence
Recorded 2026-05-09 10:05 PDT from `/Users/mike/src/worktrees/clawline-web-parity-layout-batch`:

- `npm run build`: PASS.
- `npm run test`: PASS, 23 files / 182 tests.
- `src/features/chat/MessageList.test.tsx`: covers model catalog selection dispatching typed `set_model`.

Recorded 2026-05-09 17:21 PDT after impl-agent review from `/Users/mike/src/worktrees/clawline-web-parity-layout-batch`:

- Review result: initial review found the web `/api/session-control` body was using generic `value`/`enabled` fields instead of provider/native action-specific fields. Fixed `set_model`, `set_thinking`, `set_reasoning`, `set_fast_mode`, `set_mode`, `set_verbosity`, and `cancel_current_run` request bodies.
- Review result: initial review found read-only Fast mode status went visually blank when mutation was unsupported. Fixed disabled footer controls to still show current Fast on/off state.
- `src/protocol/stream-api.test.ts`: covers `cancel_current_run` without mutation fields and action-specific `model` / `fastMode` payload fields.
- `src/features/chat/MessageList.test.tsx`: covers read-only Fast mode display while disabled.
- `npm run build && npm run test && npm run test:e2e -- playwright/tests/phase5-typing-indicator.spec.ts playwright/tests/phase5-flow-layout.spec.ts`: PASS. Build succeeded with the existing Vite large-chunk warning; unit suite passed 23 files / 184 tests; affected Playwright specs passed 7 tests.

Recorded 2026-05-09 17:24 PDT after safe main push and TARS deploy:

- Product-code commit pushed to `origin/main`: `36e23855f52928103ab56b0d055500efa2b70559`.
- TARS deploy command: `rsync -a --delete dist/ mike@tars:'/Users/mike/Library/Application\\ Support/ClawlineWeb/dist/'`.
- Smoke/hash: `http://100.85.66.60:4173/`, `/chat/test`, and `/pair` returned `HTTP/1.1 200 OK` and served `assets/index-CCACm0zW.js` plus `assets/index-B0esVyXO.css`.
- Hash match: `dist/index.html` and served `/` both `0cecf7fa395acfe45e5d253db7da47695428ba1b2f8cc9ad495772278cdde0b3`; JS both `41d28c37889806a7e1e8ec90243652dfe114f1f8f8d648ed2b7e51753a77dea2`; CSS both `a0b389e8b1b1e27706013c41f7733205e15a55f7ccc4b4f6070ab7fe83adb1ac`.
